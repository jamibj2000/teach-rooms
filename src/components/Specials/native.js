/**
 * Primitive types
 * by color and structure
 */

// variable identifier
const nativeString = "native"
const nativeNumber = 1
const nativeArray = [1,2,3]
const nativeObject = {a:1, b:2, c:3}
const nativeClass = new Date()
let nativeLetClass = new Date()


// function identifier
function whatDoesThisDo(params) {
  for (let index = 0; index < array.length; index++) {
    const element = array[index];

  }
  switch (params) {
    case 1:
      return
    case 2:
      return
    case "this note is 3":
      return
    default:
      return
  }
  return
}


export { nativeString, nativeNumber, nativeClass }
